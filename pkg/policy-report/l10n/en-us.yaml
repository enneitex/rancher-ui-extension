policyReport:
  title: Policy Reports
  headers:
    label: Compliance
    tooltip: Policy compliance status based on security and validation rules
    policyReportsTab:
      policy:
        label: Policy
      severity:
        label: Severity
      result:
        label: Result
      message:
        title: Message
      rule: Rule
      category: Category
      source: Source
  grouping:
    none: Flat List
    severity: Group by Severity
    result: Group by Result
  policyReporter:
    title: Policy Reporter
    link: Open in new tab
    noCrds:
      banner: PolicyReport and ClusterPolicyReport CRDs are not installed in this cluster. Please install a policy engine like Kyverno, Kubewarden, or Falco with Policy Reporter support.
    service:
      unavailable: Policy Reporter UI service is not available. Make sure Policy Reporter is installed and running in your cluster.
    deployment:
      unavailable: Policy Reporter UI deployment is in state "{state}". The dashboard may not load correctly.
  clusterpolicy:
    background: Background
    validationFailureAction: Validation Failure Action
  policy:
    background: Background
    validationFailureAction: Validation Failure Action
